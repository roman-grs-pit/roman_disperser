#!/bin/bash
# Set default paths for STPSF data if not already set.
# Users can override by exporting these variables before running pixi.
export STPSF_PATH="${STPSF_PATH:-$HOME/data/Roman/stpsf-data}"
